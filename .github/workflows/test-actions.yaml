name: test github actions

on:
    push:
        branches:
            - main

jobs:
    say-hello:
        runs-on: windows-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: Print hello
              run: echo "Hello, there!"


